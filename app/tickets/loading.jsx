import React from 'react'

// on this page we create a loading method to work while the page is loading or the user is fecthing data
export default function Loading() {
  return (
    <main className='text-center'>
        <h2 className='text-primary'>Loading...my man</h2>
        <p>But not for too long</p>

    </main>
  )
}
