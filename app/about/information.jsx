// we create a page here and we can access that on the url
// we can create folder insider folder
// components are SERVER components by default
const About = () => {
    return (
        <main>
            <h2>About me</h2>
        </main>
    );
}
 
export default About;